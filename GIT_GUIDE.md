# Git 使用指南

> **仓库创建日期**: 2026-02-11
> **当前版本**: v1.0-phase1

---

## 📊 仓库概览

### 当前状态

```bash
# 查看状态
git status

# 查看分支
git branch -a

# 查看标签
git tag

# 查看提交历史
git log --oneline --graph --decorate --all
```

### 分支策略

```
main        # 主分支（稳定版本）
  └─ dev    # 开发分支（日常开发）
```

---

## 🌿 分支说明

### main 分支
- **用途**: 稳定版本，每个 Phase 完成后合并
- **保护**: 不直接在此分支开发
- **标签**: 每个 Phase 完成后打标签

### dev 分支
- **用途**: 日常开发
- **规则**: 所有新功能在此分支开发
- **合并**: Phase 完成并测试通过后合并到 main

---

## 🏷️ 标签说明

### 版本命名规范

```
v<major>.<minor>-<phase>
```

### 现有标签

| 标签 | 说明 | 日期 |
|------|------|------|
| `v1.0-phase1` | Phase 1 完成 | 2026-02-11 |
| `v1.0-phase2` | Phase 2 完成（计划） | TBD |
| `v1.0-phase3` | Phase 3 完成（计划） | TBD |
| `v1.0-phase4` | Phase 4 完成（计划） | TBD |
| `v1.0-phase5` | Phase 5 完成（计划） | TBD |

---

## 📝 日常工作流程

### 开始新工作

```bash
# 1. 切换到 dev 分支
git checkout dev

# 2. 确保是最新代码
git pull

# 3. 开始工作
# 编辑文件...
```

### 提交变更

```bash
# 1. 查看变更
git status
git diff

# 2. 添加文件
git add <文件名>
# 或添加所有变更
git add .

# 3. 提交
git commit -m "描述你的变更"

# 提交消息格式建议：
# - feat: 新功能
# - fix: Bug 修复
# - docs: 文档更新
# - refactor: 重构
# - test: 测试
# - chore: 构建/工具变更
```

### 提交消息示例

```bash
# 好的提交消息 ✅
git commit -m "feat: 实现 Python 爬虫核心模块"
git commit -m "fix: 修复配置文件同步问题"
git commit -m "docs: 更新 Phase 2 实施指南"

# 不好的提交消息 ❌
git commit -m "update"
git commit -m "fix bug"
git commit -m "改了一些东西"
```

---

## 🎯 Phase 完成流程

当一个 Phase 完成时：

```bash
# 1. 确保在 dev 分支
git checkout dev

# 2. 确保所有变更已提交
git status

# 3. 切换到 main 分支
git checkout main

# 4. 合并 dev 分支
git merge dev --no-ff -m "Merge Phase X: 简要描述"

# 5. 创建标签
git tag -a v1.0-phaseX -m "Phase X 完成 - 简要描述"

# 6. 查看结果
git log --oneline --graph --decorate
```

---

## 🔍 常用命令

### 查看历史

```bash
# 简洁查看
git log --oneline

# 图形化查看
git log --oneline --graph --all

# 查看某个文件的历史
git log --oneline -- <文件名>

# 查看某次提交的详情
git show <commit-id>
```

### 对比差异

```bash
# 查看工作区变更
git diff

# 查看暂存区变更
git diff --staged

# 对比两个分支
git diff main..dev

# 对比两个提交
git diff <commit1> <commit2>
```

### 撤销操作

```bash
# 撤销工作区的变更（危险！）
git checkout -- <文件名>

# 取消暂存
git reset HEAD <文件名>

# 修改最后一次提交
git commit --amend

# 回退到某个提交（保留变更）
git reset --soft <commit-id>

# 回退到某个提交（丢弃变更，危险！）
git reset --hard <commit-id>
```

### 分支操作

```bash
# 查看所有分支
git branch -a

# 创建新分支
git branch <分支名>

# 切换分支
git checkout <分支名>

# 创建并切换分支
git checkout -b <分支名>

# 删除分支
git branch -d <分支名>

# 重命名分支
git branch -m <旧名> <新名>
```

### 标签操作

```bash
# 查看所有标签
git tag

# 创建标签
git tag -a <标签名> -m "说明"

# 查看标签信息
git show <标签名>

# 删除标签
git tag -d <标签名>

# 切换到某个标签
git checkout <标签名>
```

---

## 🚀 远程仓库（可选）

如果要推送到 GitHub/GitLab：

### 首次设置

```bash
# 添加远程仓库
git remote add origin <仓库URL>

# 推送代码
git push -u origin main

# 推送所有标签
git push --tags
```

### 日常同步

```bash
# 推送到远程
git push

# 从远程拉取
git pull

# 推送标签
git push --tags
```

---

## 📋 .gitignore 说明

已配置忽略以下内容：

- **Python**: venv/, __pycache__/, *.pyc
- **Node.js**: node_modules/, npm-debug.log
- **归档数据**: 论坛存档/, 分析报告/
- **数据库**: *.db
- **日志**: logs/, *.log
- **IDE**: .vscode/, .idea/

如需修改，编辑 `.gitignore` 文件。

---

## 🔧 Git 配置

### 查看配置

```bash
# 查看所有配置
git config --list

# 查看用户信息
git config user.name
git config user.email
```

### 修改配置

```bash
# 修改用户名
git config user.name "你的名字"

# 修改邮箱
git config user.email "your.email@example.com"

# 设置默认编辑器
git config core.editor "vim"

# 设置颜色显示
git config color.ui auto
```

---

## 📊 仓库统计

### 查看代码统计

```bash
# 查看提交数量
git rev-list --count HEAD

# 查看贡献者统计
git shortlog -sn

# 查看代码行数
git ls-files | xargs wc -l
```

### 当前仓库统计

```bash
# 文件数量
git ls-files | wc -l

# 提交数量
git rev-list --count HEAD

# 分支数量
git branch | wc -l

# 标签数量
git tag | wc -l
```

---

## 🎓 最佳实践

### 1. 提交频率
- ✅ 经常提交（功能点完成就提交）
- ✅ 每个提交只做一件事
- ❌ 不要累积大量变更一次性提交

### 2. 提交消息
- ✅ 清晰描述做了什么
- ✅ 使用统一的格式
- ❌ 不要使用模糊的描述

### 3. 分支管理
- ✅ 在 dev 分支开发
- ✅ main 分支保持稳定
- ❌ 不要直接在 main 分支开发

### 4. 代码审查
- ✅ 合并前检查代码
- ✅ 运行测试
- ✅ 更新文档

---

## 🔗 相关资源

- **Git 官方文档**: https://git-scm.com/doc
- **Pro Git 书籍**: https://git-scm.com/book/zh/v2
- **GitHub 指南**: https://guides.github.com/

---

## 📝 快速参考

```bash
# 状态查看
git status                    # 查看状态
git log --oneline            # 查看历史
git branch                   # 查看分支

# 日常工作
git add .                    # 添加所有变更
git commit -m "message"      # 提交
git push                     # 推送（如有远程）

# 分支操作
git checkout <branch>        # 切换分支
git merge <branch>           # 合并分支

# 查看差异
git diff                     # 查看变更
git show <commit>            # 查看提交
```

---

**文档结束**

**下一步**: 开始 Phase 2 开发，在 dev 分支工作
